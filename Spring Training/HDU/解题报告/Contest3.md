## 1010 组合树
**瓶颈在于暴力枚举每一条边和比较组合数大小。** 因为组合数$\binom{n}{m}$当$n$固定的的时候，$m$越接近$0$或者越接近$n$值就越小，所以从这个角度出发。每个点向点权比自己小的点看，那么不难想到把点权从小到大排序。然后可以发现，从$i=2$开始，每个点要不然和$i-1$连边，要不然和$1$连边。这样一定保证了$[1,i]$内的点都在一个联通块里面，然后取最小的边就可以得到最小生成树了。


##  注:
### 方法:
* 注意数学式子的性质
* 

### 知识:
*   组合数$\binom{n}{m}$的$m$越贴近两侧，数越小，反之越大。