#include <iostream> 
#include <algorithm>
#include <vector>
#include <cmath>
using namespace std;

/*


    还是拓扑排序判环，然后遍历环上的每一个点，

*/

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);

    int n;
    while(cin>>n){
        if(n==0) return 0;
    }
}