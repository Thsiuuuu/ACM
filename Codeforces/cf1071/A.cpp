#include <bits/stdc++.h>
using namespace std;

#define int long long 

void sol(){
    int k,x;
    cin>>k>>x;
    cout<<k*x+1ll<<'\n';
}

signed main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);

    int t;
    cin>>t;
    while(t--){
        sol();
    }
    return 0;
}